"use strict";
/* import { Request, Response, NextFunction } from "express";

export async function validateDayOff(
  req: Request,
  res: Response,
  next: NextFunction
) {
  const { data } = req.body;

  if (data) {
    console.log("passou por aqui no middleware");
  }

  next();
}
 */ 
